
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://150.158.153.204:3316/mybatis_plus_db?useUnicode=true&useSSL=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: blues
    password: blues
    type: com.alibaba.druid.pool.DruidDataSource

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    auto-mapping-behavior: full
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:com/blues/lin/mpauto/**/*Mapper.xml
  global-config:
    # 逻辑删除配置
    db-config:
      # 删除前
      logic-not-delete-value: 1
      # 删除后
      logic-delete-value: 0